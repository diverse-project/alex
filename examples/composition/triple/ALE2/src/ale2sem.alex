behavior ale2sem

import ecore "platform:/resource/ALE2/model/ale2.genmodel"

open class B {
	def void printB() {
		println("B")
		obj.rb.forEach[alg.$(it).rbPrint]
	}
}

open abstract class RB {
	abstract def void rbPrint()
}

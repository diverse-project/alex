/*
 * generated by Xtext 2.14.0
 */
package boa.xtext.tests

import boa.model.boa.File
import com.google.inject.Inject
import java.nio.charset.Charset
import java.nio.file.Files
import java.nio.file.Paths
import org.eclipse.xtext.testing.InjectWith
import org.eclipse.xtext.testing.XtextRunner
import org.eclipse.xtext.testing.util.ParseHelper
import org.junit.Assert
import org.junit.Test
import org.junit.runner.RunWith

@RunWith(XtextRunner)
@InjectWith(BoaInjectorProvider)
class BoaParsingTest {
	@Inject
	ParseHelper<File> parseHelper
	
	@Test
	def void loadModel() {
		
		val path = "example.boa"
		
		
		val lines = Files.readAllLines(Paths.get(path), Charset.defaultCharset).join(System.lineSeparator);
		
		val result = parseHelper.parse(lines)
		Assert.assertNotNull(result)
		val errors = result.eResource.errors
		Assert.assertTrue('''Unexpected errors: «errors.join(", ")»''', errors.isEmpty)
	}
}
